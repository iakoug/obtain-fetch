/**
 * 自动初始化fetch插件库并导出
 */
// import request from './init';

// require
//   .context('./plugins', true, /\.js$/)
//   .keys()
//   .reduce(
//     (plugins, modules) => [
//       ...plugins,
//       require(`./plugins/${modules.replace(/(^\.\/)|(\.js$)/g, '')}`),
//     ],
//     [],
//   )
//   .forEach((plugin) => request.use(plugin));

// export default request;
